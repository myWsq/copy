# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目目标

`copy` 是 macOS 剪贴板管理器 [Paste](https://pasteapp.io) 的开源复刻，目标是 UI 与功能的完整对标。
遇到「该怎么设计」的问题时，优先去对照 Paste 的实际行为，而不是自行发明交互。

**视觉口径已在升到 macOS 26 时变更，别再按旧口径改回去**：交互、布局、几何继续照抄 Paste，但
**材质走 macOS 26 的 Liquid Glass**，不复刻原作为兼容 macOS 14 而保留的传统毛玻璃。一句话概括
目标 —— 「用起来是 Paste，看起来是 macOS 26 上原生该有的样子」。两者冲突时：交互听 Paste 的，
材质听系统的。

范围已明确收敛（见下文「已决定的事」）：**仅 macOS、本地存储、非沙盒、Developer ID 分发**。
不要引入 iOS target、CloudKit 同步或沙盒 entitlements，除非用户明确改变范围。

## 命令

```bash
make gen      # 由 project.yml 重新生成 Copy.xcodeproj
make build    # Debug 构建（自动先 gen）
make run      # 杀掉旧实例 → 构建 → 启动
make kill     # 结束正在运行的实例
make release  # Release 构建
make clean    # 清掉 .build/、xcodeproj、生成的 plist
```

**`Copy.xcodeproj` 是生成物，已被 gitignore。永远不要手改它** —— 改 `project.yml` 然后 `make gen`。

新增源文件不需要在任何清单里登记（`project.yml` 按目录整体纳入 `App/Sources`），但**必须跑一次
`make gen` 让它进项目**。直接调 `xcodebuild` 会得到 "cannot find X in scope"；用 `make build` 就不会，
它已经依赖 `gen`。

调试日志走 `os.Logger`（`Log.store` / `Log.clipboard` / `Log.system`）。**必须写 `/usr/bin/log`
的绝对路径** —— zsh 有个同名内建命令 `log`，直接写 `log show ...` 会被它劫持，只报一句
`too many arguments`，看起来就像「日志系统是空的」。我为此误判过一次，绕了很大一圈：

```bash
/usr/bin/log show --last 5m --predicate 'subsystem == "dev.copyapp.Copy"' --info --debug
/usr/bin/log stream --predicate 'subsystem == "dev.copyapp.Copy"' --level debug
```

排查捕获问题时，直接查库能一步分清「没捕获到」还是「没渲染出来」：

```bash
sqlite3 ~/Library/Application\ Support/Copy/copy.sqlite \
  "select id,kind,substr(text,1,30),datetime(createdAt,'localtime') from clip order by id desc limit 5;"
```

## 重启 App 的正确姿势

**别用 `pkill -x Copy; sleep 1; open ...`。** `pkill` 发的是 SIGTERM，App 未必在 1 秒内退完；
此时 `open` 发现进程还在，只会**激活旧实例**，不会启动新构建 —— 于是改动看起来"没生效"，
而 `ps -p $(pgrep -x Copy) -o lstart=` 会暴露进程启动时间根本没变。务必等进程真正消失：

```bash
pkill -x Copy; while pgrep -x Copy >/dev/null; do sleep 0.2; done
open .build/Build/Products/Debug/Copy.app
ps -p "$(pgrep -x Copy)" -o lstart=    # 确认启动时间是刚才
```

## 验证 UI 改动

**默认不要自己验收 —— 改完构建并启动 App，把验证交给用户。** 他就在电脑前，自己点一次比
自动化绕一圈准确。验收标准见 `docs/acceptance.html`。

下面这套自动截图流程只在用户明确要求看效果对比时才用。注意它有两个真实缺陷：`osascript` 的
`click` 走 accessibility 的 AXPress，**测不出真实鼠标点击的行为**（`sendAction(on:)` 只对真鼠标
事件触发，用 AXPress 会误判成"功能没生效"）；触发时序靠 sleep 猜，会偶发截空。要模拟真实点击
得用 `CGEvent` 发 `.leftMouseDown/.leftMouseUp` 到目标坐标。

```bash
open .build/Build/Products/Debug/Copy.app
# accessory App 只有一个 menu bar（不是 2），status item 是它的第 1 项
osascript -e 'tell application "System Events" to tell process "Copy" to click menu bar item 1 of menu bar 1'
osascript -e 'tell application "System Events" to tell process "Copy" to click menu item 1 of menu 1 of menu bar item 1 of menu bar 1'
screencapture -x -R 0,620,1400,420 /tmp/copy.png    # -R 用逻辑坐标，原点在屏幕左上

sqlite3 ~/Library/Application\ Support/Copy/copy.sqlite "select id,kind,substr(text,1,30) from clip;"
```

**别用 ⇧⌘V 触发** —— Paste 若在运行会同时响应，两个面板一起弹出。

## 架构

数据单向流动，没有反向依赖：

```
NSPasteboard ──轮询 changeCount──> ClipboardMonitor
                                        │ PasteboardReader.read()
                                        ▼
                                   ClipStore (SQLite + FTS5 + blobs/)
                                        │ ValueObservation
                                        ▼
                                    AppState (@Observable)
                                        │
                                   ClipboardBarView (SwiftUI)
                                        │ ↩
                                   Paster ──CGEvent ⌘V──> 前台 App
```

- **`ClipboardMonitor`** — macOS 没有剪贴板变更通知，只能 0.35s 轮询 `changeCount`。
- **`PasteboardReader`** — 无状态解析器，把 pasteboard 解释成 `ClipItem`，含敏感内容过滤。
- **`ClipStore`** — 唯一持久化入口，`nonisolated` + `Sendable`，可从任意上下文调用。
- **`AppState`** — UI 只读它；所有写操作都经 `ClipStore` 再由 `ValueObservation` 回流。
- **`PastePanel`** — `NSPanel` 子类，同时负责键盘处理（SwiftUI 在非激活面板里拿不稳键盘事件）。
- **`AppAccent`** — 提取来源 App 图标主色供卡片顶栏使用，按 bundle ID 缓存颜色与图标
  （每帧去 `NSWorkspace` 查会拖垮滚动）。

## 必须知道的陷阱

这几条都是踩过才知道的，改动相关代码前先读：

1. **面板必须是 `.nonactivatingPanel`。** 它让面板能成为 key window 接收键盘，却不把 Copy 变成前台
   App。因此面板可见时 `NSWorkspace.frontmostApplication` 仍指向用户原本的 App，粘贴时无需保存/恢复
   焦点状态。换成普通 `NSWindow` 会立刻破坏整条粘贴链路。

2. **粘贴时序不能动。** `Paster.paste` 的顺序是：写剪贴板 → 收起面板 → 激活目标 App → **等 80ms** →
   合成 ⌘V。少了那个 delay，按键会打在还没让出焦点的窗口上，表现为「粘贴偶尔不生效」这种难查的偶发 bug。

3. **写剪贴板前必须 `monitor.suppressNextChange()`。** 否则自己的粘贴会被当成一次新的复制记录下来，
   形成回环。

4. **敏感内容必须跳过。** 密码管理器用 `org.nspasteboard.ConcealedType` / `TransientType` /
   `AutoGeneratedType` 标记「别记录我」，见 `PasteboardReader.sensitiveTypes`。这是剪贴板管理器的底线，
   任何改动都不能绕过这个检查。

5. **Swift 6 全局 MainActor 隔离已开启**（`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`）。UI 代码因此
   不用写 `@MainActor`，但**数据模型和跨线程类型必须显式标 `nonisolated`**，否则 GRDB 的协议 conformance
   会报 "crosses into main actor-isolated code"。`ClipItem` / `Pinboard` / `ClipStore` / `Log` 已如此处理。

6. **合成按键需要「辅助功能」权限**，无权限时 `CGEvent.post` 会被系统静默丢弃 —— 不报错、不生效。
   `Paster` 已做降级：无权限时只写剪贴板不模拟按键。

7. **热键用 Carbon `RegisterEventHotKey` 而非 `CGEventTap`。** 前者零权限、系统级可靠；后者要辅助功能
   权限、拦截全系统按键、且会被系统在超时后自动禁用需要重挂。别「现代化」这一处 —— 原作 Paste 同样
   链接了 `Carbon.framework`。热键被占用时它返回 `eventHotKeyExistsErr` 但**不会有任何运行时报错**，
   表现为「热键就是没反应」。`AppDelegate.shortcutCandidates` 因此依次回退 ⇧⌘V → ⌥⌘V → ⌃⇧⌘V，
   实际生效的组合显示在菜单栏菜单里。

8. **搜索框的键盘协调靠 `control(_:textView:doCommandBy:)`。** 搜索框必须是真 `NSTextField`（否则中文
   输入法不工作），但它默认会吞掉方向键去移动光标。`SearchField` 在文本控件处理按键**之前**截获导航类
   selector 并转发给卡片列表，其余按键放行。SwiftUI 的 `TextField` 没有等价机制 —— 不要「简化」回去。

9. **签名必须用真证书，不能用 ad-hoc 的 `-`（已配好，别改回去）。** macOS 的隐私授权（TCC）
   对签名 App 按 designated requirement 记账（bundle ID + Team ID），重新编译不影响；而 ad-hoc
   签名只能按 cdhash 记账，**每次编译都变成"另一个 App"，辅助功能授权反复作废**，开发期每启动
   一次就朝用户弹一次系统授权框。现在 `project.yml` 用 `Apple Development` + Team `8Y2J55823C`。
   另外**别在启动时调 `requestAccessibility()`** —— 启动即索权是糟糕设计，静默检查即可，
   授权入口留在菜单栏右键菜单里。

   对外分发要的是 `Developer ID Application` 证书，不在本机钥匙串里，签名与公证只在 CI 上做
   （见 `.github/workflows/release.yml`，凭据走仓库 Secrets）。本地开发不需要它。

10. **要显示中文必须在 `project.yml` 声明 `CFBundleLocalizations: [en, zh-Hans]`。** 不声明时
   `Locale.current` 会回退到 development region（en），中文系统下界面连同 `RelativeDateTimeFormatter`
   全变英文，且毫无报错。`Localized.isChinese` 另外用 `Locale.preferredLanguages` 兜底 —— 两层都要在。

11. **`NSGlassEffectView` 只能通过 `contentView` 属性挂内容，不要 `addSubview`。** 头文件明确
    只保证 `contentView` 被放进玻璃效果里，其它子视图的 z-order 行为不作保证。它另外自带
    `cornerRadius`，别再叠一层 `layer.cornerRadius` 去裁剪。多个玻璃元素并列时要包在
    `NSGlassEffectContainerView` / `GlassEffectContainer` 里 —— 玻璃无法采样玻璃，不共享
    采样区就会各算各的，视觉上对不齐。

## 视觉参数

**一律用系统语义颜色，不要硬编码数值。** 硬编码既不跟随浅/深色模式，也不响应「增强对比度」
辅助功能设置。深色模式下这些语义色的实测取值（`NSAppearance(named:.darkAqua)` 下取样所得）：

| 语义色 | 深色实测值 | 本项目用途 |
|---|---|---|
| `windowBackgroundColor` | `#1E1E1E` (white .118) | 标准窗口底，卡片**不要**用它 |
| `underPageBackgroundColor` | `#282828` (white .157) | 卡片表面 —— 要比窗口底亮一档才在玻璃上立得住 |
| `separatorColor` | white @ 10% | 卡片细描边 |
| `tertiaryLabelColor` | white @ 25% | 三级文字 |
| `secondaryLabelColor` | white @ 55% | 二级文字 |

**玻璃 tint 不是提高卡片对比的旋钮 —— 这点反直觉，实测过。** 卡片本身是深色，压暗玻璃只会把
两者拉近，tint 越深卡片越陷进背景（0.45 时几乎融为一体）。要卡片更清晰就提亮卡片表面并加描边，
玻璃反而可以更通透。当前 `glassTint` 默认 0.15，可调：`defaults write dev.copyapp.Copy glassTint 0.3`。

12. **面板复用 `NSHostingView` 靠的是 `revision` 递增，这两件事是一对，别拆开。**
    窗口 `orderOut` 期间 SwiftUI 会停止追踪 `@Observable`，隐藏时发生的数据变化不被记录，
    重新显示时它并不知道自己该刷新；而只把 `rootView` 换成新实例同样无效 —— `ClipboardBarView`
    里只有一个 state 引用，新旧实例内容完全相同，SwiftUI 判定无变化直接跳过求值。

    症状极具迷惑性：**面板永远显示隐藏前那一刻的快照，连相对时间都冻住**，而数据层一切正常
    （库里有数据、`AppState.items` 也对）。排查这类问题先分清「数据层 vs 渲染层」：往库里写
    一条再 `sqlite3` 查一下，就能立刻判定是没捕获到还是没渲染出来，省下大量瞎猜。

    最早的做法是每次 `show()` 重建 `NSHostingView`。能解决问题，但要让 SwiftUI 从头布局全部
    卡片，这段开销落在 `show()` 返回之后，观感上就是「按下热键要等一下才弹出来」。现在改为
    复用同一个 hosting view 只换 `rootView`，并让 `ClipboardBarView.revision` 每次递增 ——
    值真的变了 SwiftUI 才肯重新求值。`revision` 看起来像个没人读的无用属性，
    **删掉它就等于退回上面那个 bug**，它是复用能成立的唯一前提。

    代价是视图树被复用，`ScrollView` 会记着上次关闭时停的位置，所以 `revision` 变化时要显式
    滚回开头。

## 性能

开销集中在两处，都是受控实验实测出来的，别凭感觉改回去。埋点在 `App/Sources/System/Perf.swift`，
默认关闭：`defaults write dev.copyapp.Copy perfLog -bool YES` 打开后会输出各阶段耗时与显示期间的
帧统计（用 `NSView.displayLink` 数掉帧）。

**打开面板** —— `orderFront` 一项就占八成，它要重建近全屏窗口的后备存储、让 Liquid Glass 重新
采样背后的屏幕。所以收起时**挪到屏幕外而不是 `orderOut`**（`hide()` 里那句 `setFrameOrigin`），
并在启动时 `warmUp()` 把首次开销预付掉。

| | 首次 | 之后 |
|---|---|---|
| 改之前 | 229–290ms | 55–78ms |
| 改之后 | **9ms** | **17–23ms** |

**滚动掉帧** —— 相同滚动量下逐项开关对比：

| 配置 | 掉帧 | 最差帧 |
|---|---|---|
| 基线 | 23 | 77.3ms |
| 去卡片阴影 | 11 | 65.6ms |
| 去文字渐隐 | 8 | 57.4ms |
| 去滚动动画 | 19 | 79.3ms（无用） |
| **组合优化后** | **4** | 57.3ms |

两条结论：**给文字做渐变着色最贵**（每个字形都要参与渐变求值），换成盖一层「透明→卡片底色」的
渐变，底色不透明所以视觉完全等价，但只是普通合成；**卡片投影次之**（离屏渲染，每张可见卡片一层），
去掉后靠 `separatorColor` 描边区分即可。滚动动画不是瓶颈，不用为它牺牲手感。

13. **卡片列表用 `HStack`，不要"优化"成 `LazyHStack`。** 惰性容器在数据源大幅缩水时
    （搜索把 58 条过滤成 14 条）会算错可见范围，此后**只肯创建第一张卡片**，其余视图压根
    不存在。症状极具迷惑性：日志里 `selection`、`items` 全对，界面上却是选中框看不见、
    方向键"没反应"、鼠标点不到 —— 因为那片区域根本没有视图。给它加 `.id()` 强制重建无效。
    代价是所有条目都真的渲染，所以 `ClipStore.pageSize` 限制在 100 条。

14. **`@Observable` 的属性必须在 body 里被读到才建立依赖。** 只在 `ForEach` 闭包内部访问
    `state.selection` 是不够的 —— 闭包是惰性执行的，body 求值那一刻没碰到它，依赖就没建。
    表现为 `items` 变化能刷新、`selection` 变化却不刷新。要在 `cards` 开头先
    `let selection = state.selection` 取出来。

15. **`NSScreen.main` 对非激活的 accessory 应用会返回 nil。** 它的语义是"包含 key window
    的那块屏"，而面板唤起的瞬间 App 既不是前台也还没有 key window。`guard let screen =
    NSScreen.main else { return }` 会让 `show()` 静默退出：面板不上屏、不 makeKey、按键
    全部落空，且没有任何报错。用 `PastePanel.targetScreen`（按鼠标位置选屏 + 多层兜底）。

16. **`LSUIElement` 应用必须自建主菜单，否则标准编辑快捷键全废。** ⌘A/⌘C/⌘V/⌘X/⌘Z 是靠
    主菜单里的 Edit 菜单项分发的，没有主菜单时事件被静默转成 `noop`，搜索框里按 ⌘A 毫无
    反应。见 `AppDelegate.setUpMainMenu()`，菜单本身不会显示（面板非激活，App 不成为前台）。

17. **`icon.json` 里 `fill` 和 `fill-specializations` 不能并存。** 两个都写时深色变体被静默
    忽略，背景回退成系统黑、品牌绿整个消失，而 `actool` 不报任何错。要深色变体就只写
    `fill-specializations`，第一项不带 `appearance` 即默认外观。

    验证不用开 Icon Composer 的 GUI —— 它自带一个未公开的 CLI 可以离线渲染各种外观：

    ```bash
    ICT="/Applications/Xcode.app/Contents/Applications/Icon Composer.app/Contents/Executables/ictool"
    "$ICT" App/Resources/Copy.icon --export-image --output-file /tmp/p.png \
      --platform macOS --rendition Default --width 512 --height 512 --scale 2
    ```

    `--rendition` 可选 `Default` / `Dark` / `TintedDark`（配 `--tint-color`）。它跟着 Xcode
    版本走，升级后可能失效，但只是验证手段，不在构建链路里。

## 已决定的事（不要反复推翻）

| 决策 | 选择 | 理由 |
|---|---|---|
| 项目文件 | XcodeGen (`project.yml`) | `pbxproj` 无法可靠地被人或 AI 编辑，合并冲突频发 |
| 存储 | GRDB + SQLite FTS5 | SwiftData 没有全文索引，搜索会退化成全表 `contains` 线性扫描；剪贴板历史会长到数万条 |
| 分词器 | `trigram` | 默认的 `unicode61` 按空白切词，整段中文退化成单个 token，中文搜索直接失效 |
| 大数据 | 落 `blobs/` 文件，库里只存路径 | 截图动辄数 MB，入库会拖垮全表扫描与 FTS 重建 |
| 短查询 | <3 字符退回 `LIKE` | trigram 索引对短于 3 字符的串无效 |
| 部署目标 | macOS 26.0 | 刻意**高于**原作的 14.0：本项目优先直接用最新 API，不为旧系统写 `#available` 分支。代价是装不了本 App 的 Paste 用户很多，这是明知的取舍 |
| `meta` 与 `text` 分家 | 底栏信息单独存字段 | `text` 要留给 OCR 存图片里的文字供搜索，不能被 "1920 × 1080" 占住 |
| 面板材质 | `NSGlassEffectView`（Liquid Glass） | 面板就是一层浮在桌面上的玻璃，正是这个材质的设计用途 |
| 卡片材质 | 实色，**不玻璃化** | glass 无法采样 glass，嵌套会失真；且卡片要承载文字和彩色顶栏，玻璃会同时损害可读性与来源识别度 |
| 面板外观 | 固定 `.darkAqua` | 「选中即反白」的卡片设计依赖深色底衬托；跟随系统切到浅色会让选中态与普通态难以区分。系统 HUD 类浮层同样固定外观 |
| App 图标 | Icon Composer 的 `.icon` | macOS 26 自己渲染 Liquid Glass（镜面边、半透、投影、深色变体）。手绘进 PNG 会和系统效果叠加，且永远出不来深色/Tinted 变体。素材只给一层白色字形 + 品牌绿，重新生成见 `docs/icon/extract-glyph.swift` |
| 菜单栏图标 | app 图标同一字形的模板图 | 同源才读作同一个标识；模板图只有 alpha 有意义，系统按菜单栏明暗自动反色。不用 SF Symbol（通用符号没有辨识度），也不套 Paste 那样的圆角方框 —— 方框是为了让孤立的字母 P 成为标识，我们的 C 有缺口和箭头，轮廓本身够独特，加框只会把它挤到 11pt 把细节糊掉 |

数据落在 `~/Library/Application Support/Copy/`（`copy.sqlite` + `blobs/`）。

## 参考原作

Paste 已安装在 `/Applications/Paste.app`（v6.6.8），是最准确的行为参考 —— 拿不准交互时去实际操作它，
而不是猜。可用黑盒手段观察（`plutil -p` 读 Info.plist、`otool -L` 看链接框架、`defaults read
com.wiheads.paste` 看配置键名）。**不要反编译，不要复制它的任何素材。**

已观察到的事实：它同样是 `LSUIElement` + macOS 14.0 起 + AppKit/SwiftUI 混合 + Carbon 热键，
印证了本项目的基础选型。它链接的框架暴露了功能地图：

| 框架 | 对应功能 |
|---|---|
| Vision / VisionKit | 图片 OCR，因此图片里的文字可被搜索 |
| LinkPresentation | 链接卡片抓取标题与缩略图（**会发网络请求**） |
| QuickLookUI | 文件卡片的预览缩略图 |
| ServiceManagement | 开机启动 |
| CoreSpotlight | 剪贴板内容进 Spotlight 索引 |
| CloudKit / CoreData | 跨设备同步（本项目不做） |
| StoreKit / Sentry | 订阅与崩溃上报（本项目不做） |

配置键名显示它有四个独立可自定义热键：`activationShortcut`、`pasteStackShortcut`、
`nextPinboardShortcut`、`previousPinboardShortcut`。本项目目前只实现了第一个，且尚不可自定义。

**几何照抄，材质不照抄** —— 下面这些尺寸仍以原作为准，但玻璃材质按 macOS 26 走（见「项目目标」）。
选中态只有一圈高亮外框，卡片底色不变 —— 别被截图里某些卡片的白底误导，那是富文本内容
自带的背景色，不是选中效果（我为此判断错过一次）。

对原作截图做像素测量得到的几何（逻辑像素；2x 屏截图的数值要除以 2）：卡片 235×234、间距 21、
pitch 256、header 高 50、卡片圆角 12、面板高 332、面板四周留边 8、面板圆角 16。卡片顶栏颜色取
来源 App 图标的主色 —— 用「饱和度加权投票」而不是缩到 1×1 取平均，后者会把 Chrome 图标算成脏灰色。

## 当前进度

已打通一条**完整垂直切片** —— 复制捕获 → 存储 → ⇧⌘V 唤起 → 打字搜索 → 选择 → 粘贴回原 App。
风险最高的系统集成点都已验证，主面板已按原作截图逐项对齐几何。

**功能完成情况以 `README.md` 的 Roadmap 为唯一出处，不要在这里再维护一份。** 两份清单的更新频率
差了一个数量级（功能每做完一件就变，而本文件的架构与陷阱几乎不变），并存必然不同步。

引入网络能力（LinkPresentation 抓链接元数据）前必须想清楚：那会把用户复制的每个 URL 发给对应站点。
若实现，必须默认关闭并在设置里显式开关 —— README 目前对用户承诺了「仓库内没有任何网络代码」。
